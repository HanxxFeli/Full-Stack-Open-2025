{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook exercise for part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force build; cd ../../part2/phonebook; npm run build; @powershell Copy-Item -Recurse build ../../part3/phonebook/build",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "HF",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
